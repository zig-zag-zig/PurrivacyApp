/**
 * Re-export from shared location.
 * All consumers should eventually import directly from '../../shared/errors/authFlowError'.
 */
export { AuthFlowError } from '../../shared/errors/authFlowError';
